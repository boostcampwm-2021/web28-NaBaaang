import React from 'react';
import { storiesOf } from '@storybook/react';

import ChannelCreateModal from '@/components/ChannelCreateModal';

storiesOf('ChannelCreateModal', module).add('basic', () => (
    <ChannelCreateModal />
));
