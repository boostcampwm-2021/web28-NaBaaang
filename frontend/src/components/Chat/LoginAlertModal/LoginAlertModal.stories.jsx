import React from 'react';
import { storiesOf } from '@storybook/react';
import LoginAlertModal from './LoginAlertModal';

storiesOf('LoginAlertModal', module).add('default', () => <LoginAlertModal />);
